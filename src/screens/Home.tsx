import { View } from "react-native"
import BaseText from "../components/BaseText"

export const Home = () => {
    return (
        <View>
            <BaseText>Home</BaseText>
        </View>
    )
}